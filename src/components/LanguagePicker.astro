---
import { getLocalizedUrl, t } from '../i18n';

const nextLocale = Astro.currentLocale === 'en' ? 'fr' : 'en';
---
<div class="language-picker">
    <a href={getLocalizedUrl(nextLocale, Astro.url.pathname)}>
        {t('language', nextLocale)}
    </a>
</div>