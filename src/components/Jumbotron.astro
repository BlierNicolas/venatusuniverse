---
import { getLocalizedUrl, t } from '../i18n';

const locale = Astro.currentLocale ?? 'fr';
---
<div class="jumbotron">
    <h1>{t('heroTitle', locale)}</h1>
    <p>{t('heroDescription', locale)}</p>
    <a href={getLocalizedUrl(locale, '/stories')}>{t('heroCta', locale)}</a>
</div>